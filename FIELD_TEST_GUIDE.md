# راهنمای جامع آزمون میدانی و استقرار نرم‌افزار Gym OS در باشگاه
# Gym OS Field-Test & Pre-Club Deployment Guide

نسخه نرم‌افزار: `3.0.0-Release`  
محیط اجرایی هدف: `Windows 10 / Windows 11 (64-bit)`  
موتور ذخیره‌سازی: `Real SQLite 3 Relational Engine (ACID & WAL)`  
وضعیت سخت‌افزاری در فاز ۱۷: **سخت‌افزار واقعی غیرفعال (آماده‌سازی پایگاه‌داده و شل دسکتاپ)**

---

## ۱. پیش‌نیازهای سیستم و وابستگی‌های ویندوز (Windows Dependencies)

| نیازمندی | مقدار / وضعیت | توضیحات |
| :--- | :--- | :--- |
| **سیستم‌عامل** | Windows 10 (Build 19041+) یا Windows 11 (x64) | معماری ۶۴ بیتی استاندارد |
| **دسترسی ادمین** | فقط هنگام نصب اولیه | پس از نصب با کاربر عادی (Standard User) اجرا می‌شود |
| **فضای دیسک (Disk Space)** | حداقل ۵۰۰ مگابایت در درایو `C:` | برای فایل‌های اجرایی و فایل پایگاه‌داده |
| **حافظه رم (RAM)** | حداقل ۴ گیگابایت (پیشنهادی: ۸ گیگابایت) | بدون افت فریم در نمایش داشبورد و گزارشات مالی |
| **نیاز به اینترنت** | **کاملاً آفلاین (Offline-First)** | بدون وابستگی به CDN خارجی یا اینترنت |
| **وابستگی‌های خارجی (Node/npm/Python)** | **عدم نیاز (None)** | شل دسکتاپ به همراه فایل WASM محلی کاملاً مستقل است |
| **فایروال / شبکه** | باز بودن پورت محلی داخلی (Loopback) | برای پردازش‌های محلی شل برنامه |

---

## ۲. مراحل نصب و راه‌اندازی اولیه (Installation Steps)

1. فایل نصبی `Gym OS Setup 3.0.0.exe` را در رایانه پذیرش اجرا فرمایید.
2. مسیر پیش‌فرض نصب (`C:\Program Files\Gym OS`) را تایید و روی **نصب (Install)** کلیک کنید.
3. میانبر نرم‌افزار به‌صورت خودکار بر روی دسکتاپ و منوی استارت ویندوز ایجاد می‌شود.
4. با دوبار کلیک بر روی آیکون برنامه، Gym OS به صورت مستقل و تمام‌صفحه باز می‌شود.

---

## ۳. مراحل راه‌اندازی نخستین بار (First-Run Setup)

در اولین اجرای برنامه روی ویندوز تازه:
1. موتور پایگاه‌داده فایل فیزیکی `gym_os_production.db` را در مسیر داده‌ها ایجاد می‌کند.
2. ساختار جداول (`Schema v3`) به صورت اتمیک ایجاد و ثبت می‌شود.
3. پنجره **جادوگر راه‌اندازی اولیه (First-Run Installation Wizard)** نمایش داده می‌شود:
   - نام باشگاه (مثلاً: باشگاه ورزشی اکسیژن)
   - نام مدیر / پذیرش
   - شماره تماس و استان/شهر
   - تعریف تعداد کمدهای هوشمند اولیه
   - تعریف اولین پکیج عضویت (مثلاً: شهریه ماهانه بدنسازی)
4. پس از ذخیره، نرم‌افزار به صورت کاملاً آماده به کار باز می‌شود.
5. **مهم**: در دفعات بعدی باز شدن برنامه، سیستم به صورت خودکار داده‌های ثبت‌شده را لود می‌کند و پنجره راه‌اندازی اولیه دیگر نمایش داده نمی‌شود.

---

## ۴. ساختار مسیرهای داده و دایرکتوری‌های ویندوز (Production Paths)

تمام فایل‌های داده‌ای و اسناد باشگاه در دایرکتوری استاندارد و محافظت‌شده سیستم‌عامل ذخیره می‌شوند:

* **داده‌های اصلی و پایگاه‌داده:**  
  `%APPDATA%\GymOS\data\`  
  *(فایل اصلی: `%APPDATA%\GymOS\data\gym_os_production.db`)*
* **پشتیبان‌گیری خودکار و دستی:**  
  `%APPDATA%\GymOS\backups\`  
  *(شامل فایل‌های باینری `gym_os_backup_*.db` و مشخصات `gym_os_backup_*.meta.json`)*
* **گزارشات و رویدادهای سیستم (Logs):**  
  `%APPDATA%\GymOS\logs\app.log`  
  *(بدون ثبت اطلاعات حساس یا پسورد)*
* **تنظیمات محلی:**  
  `%APPDATA%\GymOS\config\`

> ⛔ **قانون حیاتی:** هیچ فایلی در پوشه موقت (`%TEMP%`) یا مسیر نصب برنامه (`Program Files`) جهت داده‌های مالی ذخیره نمی‌شود.

---

## ۵. دستورالعمل پشتیبان‌گیری (Backup Procedure)

### الف) پشتیبان‌گیری باینری SQLite (.db)
1. وارد بخش **تنظیمات و پشتیبان‌گیری** در منوی کناری شوید.
2. روی دکمه **«ایجاد فایل پشتیبان پایگاه‌داده»** کلیک کنید.
3. سیستم یک نسخه باینری مستقل از کل تراکنش‌ها، اعضا، کمدها و لاگ‌ها به همراه شناسنامه فایل در پوشه `backups` ذخیره و آماده دانلود می‌کند.

### ب) خروجی اضطراری جامع (Emergency JSON Export)
* در صورت نیاز به نسخه قابل خواندن انسانی، دکمه **«خروجی داده‌ها به صورت JSON»** را فشرده و فایل را بر روی فلش‌مموری نگه دارید.

---

## ۶. دستورالعمل بازیابی اطلاعات (Restore Procedure)

1. در بخش تنظیمات یا در صفحه بازیابی اضطراری (Error Recovery)، روی **«بازیابی از فایل پشتیبان»** کلیک کنید.
2. فایل باینری `.db` یا فایل اضطراری `.json` موردنظر را انتخاب نمایید.
3. سیستم پیش از انجام هر تغییری، ابتدا ساختار فایل را اعتبارسنجی می‌کند (بررسی Magic Header) و سپس یک کپی ایمن از نسخه فعلی تحت عنوان `pre_restore_*.db` ایجاد می‌کند.
4. داده‌ها با تراکنش اتمیک (`BEGIN TRANSACTION`) جایگزین و نرم‌افزار مجدداً بارگذاری می‌شود.

---

## ۷. مواردی که هرگز نباید حذف شوند (What NOT to Delete)

* ⚠️ هرگز پوشه `%APPDATA%\GymOS\data` یا فایل `gym_os_production.db` را به صورت دستی حذف نکنید.
* ⚠️ از اجرای برنامه‌های پاک‌کننده خودکار ویندوز (مانند CCleaner یا Disk Cleaners نامعتبر) بر روی پوشه `AppData\Roaming\GymOS` خودداری کنید.

---

## ۸. خاموش‌سازی ایمن سیستم (Safe Shutdown Procedure)

* جهت بستن برنامه، کافیست روی علامت ضربدر (Close) بالای پنجره کلیک کنید.
* رویداد `app:before-quit` به صورت خودکار صف نوشتن‌های معلق را مستقیماً روی دیسک فلاش (Flush) و ذخیره می‌کند.
* از بستن اجباری برنامه از طریق `End Task` در Task Manager در هنگام ثبت فاکتور خودداری فرمایید.

---

## ۹. وضعیت فعلی سخت‌افزار (Current Hardware Status)

* در فاز جاری (فاز ۱۷ - تست میدانی پایگاه‌داده و نرم‌افزار):
  - رله‌های سخت‌افزاری بازکننده قفل کمدها به صورت شبیه‌ساز امن نرم‌افزاری هستند.
  - دستگاه‌های ZKTeco و اثر انگشت بیومتریک در فاز ۱۸ با اتصال فیزیکی فعال خواهند شد.
  - ثبت تردد، صدور فاکتور، تسویه بدهی و اختصاص کمدها در سطح پایگاه‌داده SQLite به صورت ۱۰۰٪ واقعی و با پایداری کامل دیسک کار می‌کنند.

---

## ۱۰. رفع اشکال و پشتیبانی (Troubleshooting & Debugging)

| مشکل | دلیل احتمالی | راه‌حل |
| :--- | :--- | :--- |
| **برنامه باز نمی‌شود یا پیام خطا می‌دهد** | عدم دسترسی به دایرکتوری AppData یا فایل در حال استفاده | بررسی سطح دسترسی کاربر؛ اجرای مجدد با Run as Administrator در صورت لزوم. |
| **دو پنجره همزمان باز نمی‌شود** | سیستم قفل تک‌نسخه‌ای (Single-Instance Lock) | این رفتار طبیعی است تا از خرابی پایگاه‌داده جلوگیری شود. پنجره اول به صورت خودکار فوکوس می‌شود. |
| **خطای بارگذاری فایل پشتیبان** | فایل مخدوش یا تغییر فرمت یافته | از فایل‌های پشتیبان دست‌نخورده موجود در پوشه `%APPDATA%\GymOS\backups\` استفاده فرمایید. |
